[package]
name = "codegpt-runner-config"
version.workspace = true
license.workspace = true
edition.workspace = true

[dependencies]
codegpt-core = { path = "../codegpt-core" }
serde = { version = "1", features = ["derive"] }
toml = "0.8"
